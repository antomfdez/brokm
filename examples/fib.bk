// Recursive Fibonacci - functions, recursion, and printf-style printing.
I64 Fib(I64 n)
{
  if (n < 2) return n;
  return Fib(n - 1) + Fib(n - 2);
}

I64 i = 0;
for (i = 0; i <= 15; ++i)
{
  "Fib(%d) = %d\n", i, Fib(i);
}
